{
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "tsup src/index.js --format esm --out-dir dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "grammy": "^1.6.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "tsup": "^6.0.0"
  },
  "name": "telegram-bot",
  "version": "0.1.0"
}